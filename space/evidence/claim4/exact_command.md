# Exact command and environment

```bash
uv run --locked python repro/src/verify.py
```

Repository `.venv`; Python `3.12.*`; `uv.lock`; no third-party dependencies;
deterministic. Estimated and selected: one local CPU core, under one minute.
