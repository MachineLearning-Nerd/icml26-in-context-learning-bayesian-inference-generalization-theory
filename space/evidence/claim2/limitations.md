# Limitations and deviations

This falsifies Theorem 2 as written, not a cardinality-aware architecture or a
fixed-context-length theorem. The argument is asymptotic because the statement
hides constants; it contradicts every fixed finite choice rather than guessing
one. A symmetrized Lipschitz extension defines the Bayes version on
zero-probability sequences.
