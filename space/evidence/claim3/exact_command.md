# Exact command and environment

```bash
uv run --locked python repro/src/verify.py
```

Repository-level `.venv`; Python `3.12.*`; `uv.lock`; no third-party
dependencies; deterministic with no seed. Estimated and selected: one local
CPU core, under one minute; process threads: one.
