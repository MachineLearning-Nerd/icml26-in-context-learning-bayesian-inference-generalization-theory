# Limitations and deviations

This proves the upper bound, not tightness for a trained model. It depends on
the theorem's finite support/architecture constants and Hölder Bayes predictor.
Posterior variance may change between input domains even though it is
model-independent after the target domain is fixed.
