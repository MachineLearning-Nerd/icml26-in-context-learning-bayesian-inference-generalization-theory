# Method

Mean pooling and the architectural Lipschitz constants give
`Lambda_alpha`; the assumed Bayes modulus adds `L`. Bounded squared loss gives
`2(B_M+B_f)`. For any coupling, expectation difference is at most the
Lipschitz modulus times expected prompt distance; taking the coupling infimum
and averaging over `k` yields the theorem.
